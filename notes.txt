/*keyhooks*/



movement
	//check if there are walls
	//check if theres a collactable
		//collactable counter;
	//check if exit
		//check if all collactable are collacted
			//exit game if true


map checking
	//check if map is .ber
	//if ractangle
	//if sorrounded by 1
		//the outside is missing;
	//only 1 P, 1 C, 1 E

//filepath -> change to mlx->map->path;

//counter;

libft: strdup, 
printf

memory leaks when clicking ESC;
